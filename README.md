# AI-resume-analyzer
Built an AI-based web application that analyzes resumes against job descriptions and generates ATS compatibility scores with actionable improvement suggestions.Developed a resume parsing and NLP pipeline to extract structured data (skills, education, experience) from PDF/DOCX files and perform keyword matching.Cosine similarity–based text compariso
